# Working Record — append-only

Started 2026-08-18. Filename version bumps only if the *format* changes; entries append forever. Home for the counters the canon requires that had no existing home (ruling record, Entry 1; apparatus map in ballot Block 6). Everything here is capture, not ceremony — one line each, at the moment it's true.

---

## 1 · Loop count — weekly (canon 5.1 / Q1)

**Loop definitions (ruled Aug 18):** Phase 1 — a real day of Kevin's protocol runs on the new build and survives round-trip. Phase 2 — a fresh user walks a door unassisted. Post-launch — a stranger installs and completes the core action.

Format: `week of YYYY-MM-DD — N loops — one line of context`

- week of 2026-08-17 — **0 loops** — honest baseline: the new build passed its tests Aug 18, but Kevin's real daily use hasn't switched to it yet (Phase 1 done-when not met). Zero is the starting truth, not a verdict.

## 2 · Correction log (canon 5.5 / 1.14)

Ten-second entries at the moment of friction: `date — what's wrong — where it lives`. No polishing; capture now, process later. This is also the intake channel for canon amendments (ruling record).

- 2026-08-21 — Aug-20 paste-ready ledger entry was never pasted; §3 sat empty while the line lived only in the rulings file — `Session_Rulings_2026-08-20_v2.md` → fixed below, annotated.
- 2026-08-21 — facilitation applied the packet's flag discipline without derived-vs-open triage; Kevin had to push back on re-blessing his own rulings — fix: triage rule added to roadmap v1.7 working practices.
- 2026-08-21 — read-first-chain gotcha line stale ("three files deep"; now four with `FRAMEWORK_v3.md`) — `Project_Gotchas_v1.md`.
- 2026-08-21 — v1.6 Phase 0 listed quiz OD-1 as open; it was ruled Aug 18 (PHQ-8, Map Edit Log §A.1) — closed in roadmap v1.7.
- 2026-08-21 — Map Edit Log D5 (contrast module, Aug 18) vs. FRAMEWORK "cold exposure not in v1" (Aug 19) never reconciled — flagged [undetermined] in v1.7 parking lot, Kevin's line at next touch.

- 2026-08-22 — `GAPS.md` is named as CC homework in three places but has never existed: not in the repo, not in git history (`git log -S "GAPS"`, all branches), not anywhere on the machine — the only GAPS-named files belong to other projects — `Protocol_App_Roadmap_v1_7.md` (Phase 2), `FRAMEWORK.md` (open item 4), this file (Aug-21 ledger entry). Kevin's call: locate it or strike the line.
- 2026-08-22 — water still counted in "glasses" on screen four months after the April brief and two rulings (K2) — `src/app/ui/viewToday.js` → fixed this session (ounces/ml, global unit setting, schema-2 conversion of old records).
- 2026-08-22 — K4 (missed item) was dropped rather than replaced when FRAMEWORK v3 superseded PLAN §3–5; found by the drop-check, ruled same day as R16 — `docs/PLAN_3-5_dropcheck.md`.
- 2026-08-22 — roadmap layer 1 records "39 tests green"; the suite is 50 — `Protocol_App_Roadmap_v1_7.md` layer 1.
- 2026-08-23 — **closes the Aug-22 `GAPS.md` entry above**: the file did not exist, so it was written rather than located — `docs/GAPS.md`, now the standing built-vs-specced register. Three ledger rows corrected by the same pass (39 / 45 / 94 tests recorded; 97 measured), and five items `HANDOFF.md` lists as built measured as partial (D20, D22, D12, D36, Q4/Q6).
- 2026-08-23 — commit `197da3e` (menu → area → session) dropped the Plans and Supply tabs from navigation without a replacement, leaving both screens unreachable, no way to create or switch off a protocol, and a live instruction on Today pointing at the unreachable Supply screen — `src/app/ui/app.js` (dead branches at 153, 158), `src/app/ui/viewToday.js:344` → `docs/GAPS.md` §2.1.

- 2026-08-28 — S1 repo inventory ran without its canon-4.1 expectation line; the file sits on a branch that was not checked out and the requirement was unreachable at task start — this file, §3.
- 2026-08-28 — `viewLibrary.js` header says "287 items"; `library.json` holds 258. 287 is the pre-merge total; 29 duplicates are merged by name — `src/app/ui/viewLibrary.js`.
- 2026-08-28 — all 33 body-work cards carry no `muscles` field, so every release card is invisible to the library's muscle filter; 65 of 258 items are unfindable that way — `src/content/library.json` via `scripts/build-library.mjs`.
- 2026-08-28 — the catalogue's muscle names are free text: 53 strings with live duplicates (`thoracic`/`thoracic spine`, `abs`/`core`/`deep core`, `traps`/`upper traps`), not the ~44-entry census — `src/content/library.json`.
- 2026-08-28 — library items carry eight fields the plan-item validator discards (`kind`, `category`, `categoryName`, `equipment`, `muscles`, `levels`, `everyNDays`, `sides`); `viewLibrary` translates on add and drops the rest, so nothing but `levels[0].note` survives into the day — `src/lib/protocolFile.js` ↔ `src/app/ui/viewLibrary.js`.
- 2026-08-28 — `.claude/launch.json` hard-coded `C:/Program Files/nodejs/node.exe` in all four configurations; the app could not be launched from a non-Windows machine — `.claude/launch.json` → fixed same day, `node` resolved from PATH.
- 2026-08-28 — strategy v0.4 §6 prices `evidence` and `tier` as Tier 1 ("browse-side only; no code"). Every render path is a fixed allowlist — `FIELD_LABELS` in `viewLibrary.js` and `viewToday.js`, `FIELD_ORDER` in `viewSession.js` — and no view iterates item keys, so both fields would ship invisible and unsearchable. Law 5 wants epistemic status at the point of action; an inert grade does not deliver it — `Content_Library_Expansion_Strategy_v0_4.md` §6.
- 2026-08-28 — **closes the v0.4 §9 `[undetermined]` on the partner-in-frame card**: it is `rg-cr` (Contract–relax), and it was already recovered in the 18 → 13 second pass. It ships today with the caption naming the objection and answering it — "A belt, wall or doorframe does the same job solo — the partner is just what the library had." No live rejection anywhere in the repo mentions a partner, a barbell, or a dead hang — `src/data/bodywork.js`.
- 2026-08-28 — Content strategy v0.1–v0.2 §3 described Layer 2 as unbuilt; the catalogue ships 258 items. Written from project files without repo sight — corrected in v0.3.
- 2026-08-28 — "15 rejected images" carried from BUILD-NOTES into a build document; repo lineage is 18 → 13 — corrected in v0.3.
- 2026-08-28 — strategy v0.2/v0.3 stated prior image rejections stay rejected; they were a prior session's calls, never reviewed by Kevin. Rule withdrawn — a rejection is settled only once a person has seen it.
- 2026-08-28 — 225 of 258 catalogue items carry no careful text; scope corrected same day — three tiers, not one gap (strategy v0.4 §9A) — `src/content/library.json`.
- 2026-08-28 — law 1 (release never scheduled alone) has 10 of 258 items naming a load partner — `src/content/library.json`.
- 2026-08-28 — `scripts/build-library.mjs` still writes to `src/content/library.json`, which since the freeze is the *merged* catalogue; running `npm run library` now silently drops every authored item until `npm run catalog` is re-run. Left as-is because retargeting it was not on the S0b list; recorded in `Project_Gotchas_v1.md` §4 and flagged for Kevin — `protocol-app/scripts/build-library.mjs`.
- 2026-08-28 — `fields.carefulAudience` in the PT module is dropped when an item is added to a day: the validator filters `fields` to the five K3 keys, so the gating information D29 needs does not travel. Reported rather than repaired — the careful-tier model is S4 — `src/lib/protocolFile.js` ↔ `src/content/authored/vestibular.json`.
- 2026-08-28 — a catalogue item's own `target` is ignored by the add flow, which synthesises 3×10 or 45 seconds; the PT's 30-second drills would land in a day saying 45 — `src/app/ui/viewLibrary.js`.
- 2026-08-28 — **closes the `npm run library` entry above** (Kevin's call 1): ruled D24's own class rather than new scope, since a silent-data-loss path is what the law forbids by name. The generator now writes `imported-legacy.json`, refuses without `--re-freeze`, and stamps the `master` sha from git instead of a typed constant — `protocol-app/scripts/build-library.mjs`, `tests/catalog.test.js`.
- 2026-08-28 — **closes the `fields.carefulAudience` entry above** (Kevin's call 2): the tag now rides beside `fields` rather than inside it, so the five K3 keys stay five; the audience list is left open per D40 (census, not cap). `build-catalog.mjs` halts on the old placement rather than shipping a field that does nothing — `src/lib/protocolFile.js`, `scripts/build-catalog.mjs`.
- 2026-08-28 — **closes the synthesised-target entry above** (Kevin's call 3, ranked highest): the add flow no longer invents `3 × 10` or `45 seconds`. It uses the item's target when there is one and asks for nothing when there is not. Canon 3.7 in code — the numbers were never derived, and the PT's 30 seconds would have been overwritten by 45 in the one module built to be shown to him — `src/app/ui/viewLibrary.js`.
- 2026-08-28 — two files named `Project_Gotchas_v1.md`: the repo copy written this session and a project-knowledge copy holding five entries the repo copy lacks (file:// double-click trap · frozen session view · chat is not storage · roadmap versions sharing a date · read-first chain depth). Titles recorded in the repo copy so they cannot be lost; bodies not written, because writing them from titles would invent the contents of a document that already exists. **The repo copy is incomplete and not yet authoritative** — `protocol-app/docs/Project_Gotchas_v1.md`.
- 2026-08-28 — `tier`, `role` and `evidence` still render nowhere (Kevin's call 4, left for Phase 2). Noting the collision rather than the field: the vestibular module already carries `evidence` blocks and is the content most likely to be shown to the PT first, so "before anyone outside sees it" and "soon" are the same date — `src/app/ui/viewLibrary.js`.
- 2026-08-28 — **closes the call-4 entry above** (Kevin reversed his own caveat the same day): `tier` now sits on the closed library card face and `evidence` renders with its basis when the item opens, both searchable; `role` deliberately left unrendered as composer machinery rather than a label. Three-state absence preserved — an item with no tier shows none rather than a default. **Residual law-5 gap recorded rather than closed:** the grade does not travel into a person's day, because the validator does not keep `evidence` — visible while browsing, gone once the item is yours — `src/app/ui/viewLibrary.js`, `Project_Gotchas_v1.md` §5.
- 2026-08-28 — **re-prices and mostly closes the law-5 residual above** (Kevin's reframe): the *tier* now travels into the day — validator keeps it, add flow carries it, and it renders on the Today card and in the session runner — while the graded basis stays in the library as reference. Canon 3.8's erosion mechanism running across surfaces rather than document generations: browse said "worth trying", the day said nothing, and by the fortieth repetition the drill is just something you do. An unknown tier is kept and warned about, never dropped; an untiered item is labelled nothing — `src/lib/protocolFile.js`, `src/app/ui/{viewLibrary,viewToday,viewSession}.js`.
- 2026-08-28 — tap-through from a day item to its grade: **priced, not built.** Cheap on the Today card (a `query` option on `viewLibrary` plus one callback through `viewToday`'s existing options bag); a design question in the session runner, which owns the whole screen by intent and would need a ruling on suspend-vs-discard before it could link out. Recorded so the price does not have to be rediscovered — `protocol-app/docs/Project_Gotchas_v1.md` §15.
- 2026-08-28 — a correction-log entry whose named target does not exist has no natural moment of rediscovery: the 21 Aug read-first-chain entry idled a week because the file it pointed at had not been written. Nothing in the process noticed. Suggests a periodic sweep of §2 for entries naming files that do not exist — `protocol-app/docs/Project_Gotchas_v1.md` §14.
- 2026-08-28 — **port defect, live since the port: 22 of the 33 body-work cards ship with no instructions.** `scripts/build-library.mjs` reads `tool · release · load · notice · careful`, but only 10 source cards use that shape. The other 23 use `steps · dose · feels`, and **none of those three keys is read** — 8,688 characters of step-by-step instruction, 23 doses and 10 sensation notes sit unused in `src/data/bodywork.js` on `master`. One card (`CO2 tolerance`) is rescued by a `GENERALISE` override, so the shipped count is 11 with instructions, 22 without. A card that says what tool to use, what to notice and what to be careful of, and never what to do — `protocol-app/scripts/build-library.mjs`, `src/data/bodywork.js`.
- 2026-08-28 — **the balance collision is a symptom of the above, not a duplicate.** `bw-bl-prog` "Single-leg progression" is not a stub someone wrote thinly: its source carries a **five-rung ladder** (single leg eyes open → eyes closed → head turns → folded towel → towel eyes closed) plus the dose "5 minutes, most days. Barefoot always." All of it dropped in the port, which is why its careful text cites a "Level 3" the app cannot show. `bw-bl-toes` likewise has three real drills (toe spread, big-toe lift, towel scrunch), all invisible. The reconciliation Kevin flagged is between two ladders, one currently unreadable — not between a ladder and an empty card — `src/content/library.json`.
- 2026-08-28 — the module ordering the vestibular file specifies **cannot be honoured**: `build-catalog.mjs` sorts every item by name across the whole shelf and the library browser has no module or section view, so the protective card `dizziness-which-kind` — written to sit ahead of every drill because it changes whether some of them are appropriate — lands **10th of the module's 14**, after six of the drills it gates. No ordering field exists on an item — `scripts/build-catalog.mjs`, `src/app/ui/viewLibrary.js`.
- 2026-08-28 — `sourceNote` (the app-content-vs-PT-prescribed split, on 5 items) renders nowhere and is dropped by the validator. The provenance line Kevin names as the one he'd least want blurred when handing the module to his PT is currently invisible on every surface — `src/app/ui/viewLibrary.js`, `src/lib/protocolFile.js`.
- 2026-08-28 — the screens test's "every item says something about what to do" check (`fields.release || why || levels`) passes on `why`, which for body-work items is the **section note shared across the section**, not per-item instructions. That is why 22 instruction-less cards have been green since the port — `protocol-app/tests/screens.test.js`.
- 2026-08-28 — **closes the assertion entry above (call 1, done first on Kevin's ruling that the hole matters more than the defect).** The check no longer accepts `why` unconditionally: a `why` string that appears on more than one item is the section talking, not the item, and cannot be its instructions. Run against the unfixed catalogue it went red naming all 22 cards; after the port fix, green at 33 of 33. The 19 daily practices stayed green throughout — their `why` is genuinely per-item — `protocol-app/tests/screens.test.js`.
- 2026-08-28 — **closes the port-defect entry above (call 3).** `build-library.mjs` now reads `steps → release`, `feels` appended to `notice`, `dose` to a real catalogue field, and a ladder encoded in steps to `levels`. Re-frozen: `imported-legacy.json` hash `53c4e786b5aa` → `adbc89d0808d`, still `master@4f631ae`; catalogue rebuilt. **33 of 33 body-work cards now carry instructions, 23 carry their dose, and `bw-bl-prog`'s five rungs are readable.** Doses ported as written; provenance tags are S1's job per Kevin's call — `protocol-app/scripts/build-library.mjs`, `src/content/imported-legacy.json`.
- 2026-08-28 — **closes the `sourceNote` entry above (call 2).** Provenance renders on the library card as "Source", beside the tier and the evidence grade, and is searchable. Verified in the running app on `gaze-stabilization`: tier, evidence and source all visible on one card — `src/app/ui/viewLibrary.js`.
- 2026-08-28 — the 23 ported doses needed somewhere to go: `dose` renders on the library card, and the add flow now prefers an item's real `dose` over the chosen rung's note, falling back to the rung note as before when there is no dose. A dose and a rung are different things and this keeps them apart — `src/app/ui/viewLibrary.js`.
- 2026-08-28 — `bw-bl-prog`'s five rung *names* are the source sentences split at their first sentence boundary, not curated labels — deliberately not tidied, because a shorter label would be one the generator invented about somebody's own drill. Worth shortening by hand at the ladder reconciliation — `protocol-app/scripts/build-library.mjs`.
- 2026-08-28 — **not done, deliberately:** the two balance ladders are NOT merged. `bw-bl-prog` (5 rungs, folded towel, barefoot-always) and `balance-standing-ladder` (7 rungs, wobble boards) are now both readable, which is the condition Kevin set for making that call with both texts visible. Ordering field + module view (call 4) also not started — ranked last, and the per-item `carefulAudience` guards are the safety net rather than the card's position — `src/content/library.json`.
- 2026-08-28 — constraint to carry into that reconciliation, stated by Kevin and recorded here so it does not live only in a transcript (canon 1.7): the interesting difference between the two ladders is not the rungs. The legacy card treats **barefoot as a rule** and reaches for a **folded towel** where the authored one reaches for wobble boards — which is the better instinct for an app whose users may own nothing. Keep it in the merge — `src/content/library.json`.
- 2026-08-28 — release library session 1 loaded: 5 files, **77 items**, no halt. Catalogue 284 → **361** (258 imported + 103 authored). Delta's counts verified against the data rather than restated: 49 role=release items and **all 49 carry a loadAfter**, 11 activation items, 14 technique guides — `src/content/authored/release-*.json`.
- 2026-08-28 — nerve-glide file loaded: 15 items, no halt. Catalogue 361 → **376**. Verified rather than restated: 10 role=nerve-glide items and all 10 carry a loadAfter; 15 distinct evidence families; 0 evidence blocks without a grade — `src/content/authored/nerve-glides.json`.
- 2026-08-28 — **answers nerve open call 4 (category id).** `nerve` lands correctly: 17 items on one category, one `categoryName` ("Nerve glides") across all of them including the two legacy cards. No split, unlike `release`, which still carries two display names — `src/content/library.json`.
- 2026-08-28 — **answers nerve open call 3 (new role value).** `nerve-glide` was not coerced and not rejected, because **nothing in the app reads `role` at all.** Seven role values now ship (release 60, awareness-cue 16, technique-guide 15, activate 11, nerve-glide 10, balance 3, stretch 3) and all seven are inert — `src/content/library.json`.
- 2026-08-28 — **corrects the nerve delta's swap-group counts.** It records median and sciatic as groups of 3 "with legacy". They are groups of **2**: the imported cards carry no `swapGroup` at all — the field does not exist on any imported item — so `bw-nv-median` and `bw-nv-sciatic` sit in the same category but are members of nothing. The "no swap group of one" criterion still holds, for a different reason than the one recorded — `src/content/library.json`.
- 2026-08-28 — the delta records 14 unresolved cross-file `loadAfter` holes across both sessions; measured **16** (14 from the release files plus the 2 grip/hand ones this file adds). 43 id references resolve — `protocol-app/tests/catalog.test.js`.
- 2026-08-28 — **nerve open call 5 is bigger than one card, and the guard that should have caught it is a wordlist.** `bw-nv-median` is not alone: **five imported items ship text that presumes the reader has a diagnosis**, all in the same construction — `bw-nv-median` "Given the thoracic outlet picture", `bw-br-co2` "given the orthostatic picture", `bw-rg-ecc` "given the post-exertional picture", `bw-aw-press` "Given the TMJ picture", `bw-bl-prog` "given the dysautonomia". PLAN §1 says a stranger gets an app, not one person's regimen. The screens test that exists to stop this checks four hardcoded phrases (`rho `, `boost blenz`, `the right hip`, `surgically altered`) — a fixed list cannot catch a phrase nobody thought to add to it, which is the same shape as the `why` assertion. **All five are imported and frozen; every authored file is clean.** Not repaired: fixing them means a `GENERALISE` override plus a re-freeze, which is a content edit needing Kevin's ruling — `src/data/bodywork.js`, `protocol-app/scripts/generalise.mjs`, `protocol-app/tests/screens.test.js`.
- 2026-08-28 — **closes the entry above (Kevin: fix the five, re-freeze, make the test check the pattern).** Assertion first, as ruled for the port fix: run against the unfixed content it went red on **both** shipped surfaces — the catalogue and, newly established, `starter.json`, so the shipped day carried the same five. Five `GENERALISE` overrides added (`br-co2`, `rg-ecc`, `nv-median`, `bl-prog`, `aw-press`), each keeping its caution in full and stating the condition as a case rather than a fact, in the idiom the `hip` and `pec` entries already used. Rebuilt both surfaces: `starter.json` seed `8e0ffda37817`, `imported-legacy.json` `adbc89d0808d` → **`c52e79db9860`** (still `master@4f631ae`), catalogue `f180da94ffd7`. Green — `protocol-app/scripts/generalise.mjs`.
- 2026-08-28 — the new guard checks a **construction, not a condition list**: `given the|given your` + a noun, plus `since/because you have` and `because of your`. All five leaks used the same presupposition trigger; the three innocent uses of "given" in the catalogue take no definite article (`given nothing to do`, `given how rarely`, `any given episode`) and are not matched. `populationCareful` is exempt by design — it is shown only to people who self-selected into that population (D29), so naming the condition there is the point of the field — `protocol-app/tests/screens.test.js`.
- 2026-08-28 — **known boundary of that guard, stated rather than left to be discovered:** it scans instruction text (`fields.*`, `why`, `dose`, `notes`) and **not `sourceNote`**, which also renders on the card. One authored `sourceNote` matches the pattern (`visual-motion-tolerance`: "worth asking whether it fits his plan given the orthostatic findings"), and separately **7 of 97 sourceNotes name Kevin** — 5 as "not from Kevin's PT" against 92 using the house phrasing "App content, not clinician-prescribed", plus 2 authoring asides ("Kevin's call") that render to a stranger. `starter.json` is clean of the name. Not repaired: whether provenance notes addressed to the author and his clinician should render to strangers at all, and in what words, is a design question Kevin opened and has not settled — patching it inside a guard would settle it silently — `src/content/authored/vestibular.json`, `src/app/ui/viewLibrary.js`.
- 2026-08-28 — **parked, not resolved: `populationCareful` is two fields sharing one name.** Design conversation with Kevin, recorded here because it exists nowhere else (canon 1.7). Ruled in passing: the gate should be an opt-in offered to everyone rather than self-selection by population — a person who wants extra care says yes without having to claim a diagnosis, adjustable in settings. Two things then fell out and both are open. **(a)** Wording that names populations *or their symptoms* excludes the elderly, post-op, pregnant and deconditioned readers the layer would also help; framing it by what the notes *do* ("smaller ranges, seated versions, what to do on a bad day") does not. **(b)** Better still, and where we landed: do not hide it — **fold it**, and let the setting control open-by-default rather than show/hide. That removes the onboarding question entirely, which matters because no onboarding flow exists to hang one on; `ui.weeklyCount` is the precedent for the setting itself.
- 2026-08-28 — **the measurement that makes the above concrete, and the part that must not be flattened.** 84 of the 91 items carrying `populationCareful` also carry a `levels` ladder, and in spot-checks the population text *describes rung 1 verbatim* — "standing against a wall gives you a pressure dial that side-lying on a roller does not" beside a rung 1 of "Ball against a wall — standing, pressure fully controlled". So most of the field collapses into the ladder that already exists, already travels into the day via `activeLevel`, and already renders. **But 13 of the 91 do not say "go gentler" — they say do not, or ask first** (`tech-percussion` "do not use it on or near a joint that subluxes" · `nerve-sciatic-standing-step` "skip this version and raise it with a clinician" · `release-subscapularis-armpit` "belongs with a clinician" · `release-pelvic-floor-supported-positions` "skip it or use the lying versions" · and 9 more). There is no rung of compression flossing that is safe for unstable blood pressure. Modelling those 13 as "difficulty 1" turns a stop into a start-easier, which is the most dangerous mistranslation available in this library. **If the merge into `levels` happens, those 13 must be separated out first and moved to the universal careful text, ungated and unfoldable.** A guard for that boundary should exist before the merge, not after — `src/content/library.json`, `src/app/ui/viewLibrary.js`.
- 2026-08-28 — **answers nerve open call 6 (dose inconsistency), confirmed live.** Inside one shelf: `bw-nv-median` and `bw-nv-sciatic` display "10 reps × 2 sets per side" with no provenance on file, while all 10 authored glides display "[undetermined]" beside them. The technique guide states plainly that the legacy number is not derived from anything on file. Visible to a reader as two different confidence levels for the same drill family — `src/content/library.json`.
- 2026-08-28 — **answers open call 5 (cross-file loadAfter).** All **35** id references resolve, including **21 cross-file** ones (`load-scap-setting` referenced by 7 items from another file, `load-deep-abdominal-connection` by 6), plus 14 deliberate `id: null` holes naming their target. Pinned by a test. **But nothing resolves them at runtime because nothing reads `loadAfter` at all** — the references are internally consistent and inert until the composer exists — `protocol-app/tests/catalog.test.js`.
- 2026-08-28 — **answers open call 2 (new field names).** Nothing was stripped; the merge filters no fields. Read by something: `dose` (renders in the library and travels into the day), `sourceNote` (renders in the library). Read by nothing: `technique`, `swapGroup`, `regions`, `context`, `loadAfter`, `populationCareful`, and the `role: technique-guide` value. The first five are correctly Tier 3 — composer machinery, inert by design. `populationCareful` is not: see below — `src/content/library.json`.
- 2026-08-28 — **`populationCareful` is the §9A gating model implemented in data and invisible in the app.** 76 items carry it and no surface renders it; the validator drops it, so it does not reach a day either. Unlike the other inert fields this one is safety content — hypermobile dosing, orthostatic positioning, PEM pacing — written per item and reaching nobody. Same shape as `carefulAudience` before 28 Aug, and the larger half of it — `src/app/ui/viewLibrary.js`, `src/lib/protocolFile.js`.
- 2026-08-28 — **law 5 inverted by a dangling pointer, found and fixed.** The release library grades evidence once per technique family and points items at it with `evidence.inheritsFrom`. Nothing followed the pointer, so **48 items were about to render "Evidence: not graded" while a resolvable grade sat one hop away** — a claim about the evidence rather than a gap in it, and worse than saying nothing. `build-catalog.mjs` now dereferences at build time (keeping `inheritsFrom` visible for provenance) and halts on a reference that leads nowhere; the card no longer prints a fallback. 87 of 87 evidence blocks now carry a real grade — `scripts/build-catalog.mjs`, `src/app/ui/viewLibrary.js`.
- 2026-08-28 — one category, two display names: `release` is labelled **"Release & load"** on the 10 imported cards and **"Release & reconnect"** on the 77 authored ones, so the library shows both strings for one shelf. Not a build error and not repaired — the naming is Kevin's call — `src/content/library.json`.
- 2026-08-28 — 71 of the authored items carry a `dose` whose value is or contains the literal `[undetermined]`, which now renders as a visible "Dose [undetermined]" line. That is canon 3.7 working as designed — a hole shown rather than a plausible number invented — but it is 71 cards' worth of visible blank and worth a display decision — `src/app/ui/viewLibrary.js`.
- 2026-08-28 — **closes the two-files-named-Project_Gotchas entry above**: Kevin supplied the project copy's five entries verbatim; merged as §§9–13. One merged corrected — the read-first chain read "three files deep" and is four (canon → ruling record → roadmap opener → FRAMEWORK v3) — which also **closes the 2026-08-21 entry** in this section that flagged exactly that staleness and was never applied — `protocol-app/docs/Project_Gotchas_v1.md`.

- 2026-08-29 — `GAPS.md` §1 records the library browser as "258 catalogued items"; `library.json` holds **376** (258 frozen legacy + 118 authored). The figure predates the authored merge and is stale by 118 — `docs/GAPS.md` §1.
- 2026-08-29 — `category` (26 values) answers **seven** different questions at once: what it does (release, vagal, recovery, detox, range, mobility, core) · what body part (legs, ribs, airway, nerve, vestibular) · what movement pattern (push, pull) · what equipment (kettlebell, mace, jump_rope) · what tradition (martial_arts, athletic) · where it sits in the UI (entry-points) · whether it is a practice at all (measure). The cost is not tidiness: a kettlebell swing and a mace 360 are filed under their tools, so nothing can see that both are load, and law 1 cannot be enforced against content the composer cannot classify — `src/content/library.json` → `docs/TAXONOMY.md` §1.
- 2026-08-29 — `kind` (bodywork · stretch · exercise · practice · selftest) is the five source files of the 2025 app, and it already disagrees with itself: `release` splits 76 bodywork / 11 exercise, `mobility` splits 31 stretch / 6 exercise, `vestibular` splits 10 practice / 4 exercise. Same work, two shelves, decided by which file it arrived in — `src/content/library.json` → `docs/TAXONOMY.md` §1, T7 (retire it).
- 2026-08-29 — the free-text muscle names logged 28 Aug at 53 strings are now **94**, and the drift is the same shape: live synonym collisions (`quads`/`quadriceps`, `piriformis`/`deep hip rotators`/`deep hip rotators (piriformis group)`, four spellings of trapezius), twelve values that are not muscles (`posture`, `nervous system`, `full body`, `grip`, `anti-rotation`, `ankle stability`, `cervical movement sense`, `oculomotor`, `core`, `back`, `legs`, `spine`), and **three that are fascia sitting in the muscle field** because there was nowhere else to put them (`IT band`, `plantar fascia`, `thoracolumbar fascia`). A field that grew 53 → 94 in a day of authoring is a vocabulary with nothing holding it — `src/content/library.json` → `docs/TAXONOMY.md` §3.
- 2026-08-29 — **all thirteen self-tests carry zero `regions` and zero `muscles`.** Not sparse — empty. Knee to wall does not record that it measures ankle dorsiflexion, so no test can route anybody toward anything, which is the entry the whole system needs for a person who only knows "my elbow hurts". Compounds `GAPS.md` §3's finding that all thirteen are `tracking: 'check'` — `src/content/library.json` → `docs/TAXONOMY.md` §5.
- 2026-08-29 — `equipment` is two vocabularies in one field: 263 items carry short enum-ish values (`bodyweight` 94, `band` 24, `dumbbell` 20, `kettlebell` 10) from the exercise import, and 71 carry prose written for a reader ("A wall, plus a rolled hand towel or a small inflatable ball…"). The prose is instruction and belongs in `fields.tool`; only the first kind can ever be filtered on — `src/content/library.json` → `docs/TAXONOMY.md` §2.6.
- 2026-08-29 — the 119 items in the shipped day protocols carry **no facets at all**: their entire field set is `id, name, dose, why, tracking, target, fields, cadence, photos`. Extends the 28-Aug entry on dropped fields — the list is ten (`kind`, `category`, `role`, `regions`, `muscles`, `equipment`, `context`, `swapGroup`, `loadAfter`, `nerves`), and `loadAfter` on 67 items is law 1 already written down as data, not surviving contact with the day. Whatever taxonomy wins, the composer would deal from a deck with no suits — `src/content/starter.json`, `src/app/ui/viewLibrary.js`, `src/lib/schema.js` → `docs/TAXONOMY.md` §9.3.
- 2026-08-29 — **not a defect, recorded so it is not "fixed" later:** eleven items carry `technique: null` explicitly, every one a `load-*` item. That is an explicit "has no technique", not an unanswered question — D24's three-state discipline kept by hand in authored content. The facet model preserves it — `src/content/authored/`, `docs/TAXONOMY.md` §2.5.

- 2026-08-29 — **law 7 stated and broken in the same file.** `scripts/day-arc.mjs` line 18 says "The app never schedules inside somebody's coffee; it schedules alongside it", and line 89 named the block **"While the kettle boils"** — one person's ritual handed to every reader as structure. `FRAMEWORK.md` §2 rules it explicitly: "The anchor ritual itself belongs to the user; the app schedules around it, never inside it." It is law 11 in a second costume — nobody's case earns schema, including when the case is a kettle rather than a diagnosis. Fixed same day: the block is "While you're already up"; the kettle stays in `fields.tool` as an example, where it was already correct — `scripts/day-arc.mjs` → `docs/TAXONOMY.md` §6.1.
- 2026-08-29 — the cost of the above was not the name (Kevin: "it wasn't necessarily meant to be its own thing... we have focused on that at the sacrifice of the greater and more important principle"). `FRAMEWORK.md` §4's three layers — awareness, movement snacks, ambient practices — **are the principle, and all three are at zero**: `day-arc.mjs` shipped arc parts 1, 2, 5 and 6 and deferred 3 and 4 to the composer. The example got promoted to structure precisely because the structure was missing — `docs/GAPS.md` §1 layer 2, `docs/TAXONOMY.md` §6.2.
- 2026-08-29 — **an authoring trap caught by writing its rule down, recorded because it reads perfectly when wrong.** `opportunities.json` first had brushing your teeth occupy `one-hand`, which under set-disjointness leaves two-handed work matching a moment where one hand is holding a toothbrush. `occupies` is what a moment makes *unavailable*, and the hand values are ordered: anything blocking one-handed work has already blocked two-handed work. `check-vocab` now refuses `one-hand` without `hands` — `src/content/vocab/opportunities.json`, `scripts/check-vocab.mjs`, `docs/TAXONOMY.md` §6.3.

- 2026-08-29 — **a review row is often a missing node, not an ambiguous string.** The anatomy seed mapped `glutes` (41 item tags) to `glute-max` and flagged it for a human, along with `hip abductors`, `grip` and three rotation strings. Kevin's correction — the group *and* the specifics, and actions too — closed six of the ten review rows by adding nodes rather than by deciding anything: a `glutes` group node holds maximus and medius/minimus, and `action` nodes with a `producedBy` edge gave the movement-named strings somewhere exact to land. Review rows 10 → 5. Worth carrying into the fold-in's remaining rows and into every future vocabulary: check whether the ambiguity is in the string or in the graph — `src/content/vocab/anatomy.json`, `docs/TAXONOMY.md` §3, §3.1.
- 2026-08-29 — search needs **two walks in opposite directions** and the seed shipped one. `rollUp` (tag → ancestors) makes an item tagged `glute-med-min` findable under "hip"; `expand` (query → descendants, plus `producedBy` for actions) makes a query for "glutes" return both glutes. With only the first, asking for glutes misses every specific — which is precisely what Kevin caught — `scripts/check-vocab.mjs`, `docs/TAXONOMY.md` §3.

- 2026-08-29 — **the `core` review row was diagnosed wrong and the correction came from reading the items.** The fold-in note said the catalogue used it "for both the canister and the abdominal wall". All 36 items carrying it are exercises — kettlebell, mace, carries, planks, strikes, Bird Dog, Pallof Press — and in every one it means holding the trunk still while force passes through it. The data settles it three ways: items meaning the abdominal wall already say `abs` or `obliques` and add `core` on top; `ex-pallof-press` carries `core` and `anti-rotation` together; and no release or bodywork item uses `core` at all. Resolved to a `trunk-bracing` action, not to either area node — `src/content/vocab/anatomy-foldin.json`, `docs/TAXONOMY.md` §3.2.
- 2026-08-29 — the missing-node lesson held for four more rows: `traps` needed a `trapezius` group exactly as `glutes` needed one, `deep hip rotators (piriformis group)` stopped needing a decision once the group had children to expand into, and `core`/`anti-rotation` needed an action. Review rows 5 → 4, and every survivor now carries exactly one item tag. A test pins that: an unresolved row with more than four item tags fails the suite, so a heavy judgment call cannot sit in the worklist unnoticed the way `core` and `glutes` did — `tests/vocab.test.js`.
- 2026-08-29 — **expansion is one-directional and that is a deliberate limit, not an oversight.** An action reaches the muscles that produce it; a muscle does not reach the actions it produces, so a search for "the canister" does not return the Pallof Press. Chaining the other way over-broadens immediately — glute max would drag in hip extension and from there the hamstrings. If it matters, the fix is a single-hop muscle-to-action lookup, never transitive expansion — `scripts/check-vocab.mjs`, `docs/TAXONOMY.md` §3.2.

- 2026-08-29 — **`schemaVersion` was written into every export and never once read.** `protocolFile.js:376` stored it and no code path compared it to anything, so a file from a newer app imported silently and every field this version had no slot for went with it. Carrying on with less while looking fine is the failure D24 forbids by name. Closed with the schema-3 rung: `validateFile` warns and still imports, because most of a newer file is ordinary — `src/lib/protocolFile.js`, `docs/TAXONOMY.md` §9.1.
- 2026-08-29 — a name collision found while writing the rung: TAXONOMY names the anatomy facet **target**, and `target` on an item has meant sets/reps/seconds since PLAN §4.2. Stored as `anatomy`; the older meaning keeps the key. Worth catching before 376 items carried it — `src/lib/schema.js`, `docs/TAXONOMY.md` §9.1.
- 2026-08-29 — **the catalogue shipped for one build with the version string of content that no longer existed.** `mergeCatalog` hashes the items and returns; the anatomy fold-in ran afterwards in `main()`, so 289 newly tagged items went out under the untagged catalogue's version. Same class as the `seedVersion` that never changed and stopped shipped content reaching anybody: a cache key that does not move when the content does. `versionOf` is now exported and re-applied after the fold-in, and `tests/catalog.test.js` computes the expected version through the fold-in too — `scripts/build-catalog.mjs`.

- 2026-08-29 — **legacy was allowed to win a name on the strength of being old, and Kevin reversed it the same day.** The anatomy facet is called `target` in TAXONOMY §2.1; `target` on an item had meant sets/reps/seconds since PLAN §4.2; the first answer was to rename the facet to `anatomy` so the older meaning could keep the key, and the commit message said so approvingly. Kevin: "Can we not reformat things to fit the current structure rather than allowing older versions of things to dictate what is happening now?" Ruled as a standing rule — the current structure names things, older versions are reformatted to fit. The dose is `amount` now, the schema-3 rung renames it on stored records (editable only because that rung is a day old and unreleased), and `validateFile` reads either key so old backups still import — `src/lib/schema.js`, `src/lib/protocolFile.js`, `docs/TAXONOMY.md` §10.1.
- 2026-08-29 — **the rename found a silent overwrite that was already live.** Four items in the vestibular module carry `target: { seconds: 30 }` — the clinician's own dose, the one the 28-Aug correction log records as the reason the add-flow stopped inventing 45 seconds. `applyFoldin` writes `target` as a list of anatomy ids, so building the catalogue replaced that dose with `["neck"]` and said nothing. Caught by a test, not by reading. The authored file now uses `amount`, and `build-catalog` halts on an object `target` rather than eating it — `src/content/authored/vestibular.json`, `scripts/build-catalog.mjs`, `tests/catalog.test.js`.

- 2026-08-29 — **the `mobility` shelf was two effects wearing one name, and the ledger would have spent the wrong one.** 37 items: 23 are positions held at end range (Pigeon, Couch Stretch, Downward Dog — `lengthen`) and 11 are joints driven through their range (CARs, cat-cow, 90/90 switch — `mobilise`). Derivation from `category` alone would have counted every static stretch as movement through range, in the one facet the coverage ledger and the pairing law both spend. Split in `facet-overrides.json` as reviewable data with a stated rule per row — `src/content/vocab/facet-overrides.json`, `docs/TAXONOMY.md` §10.2.
- 2026-08-29 — one item in that shelf was never a stretch: `st-legs_up_wall` is filed under mobility, lengthens nothing, and is the restorative position closing the evening flow — `calm` + `circulate`. Found by reading 37 names rather than by any rule — `src/content/vocab/facet-overrides.json`.
- 2026-08-29 — **`kind` retired; `category` kept, with the reason written down.** `kind` was the five source files of the 2025 app and carried nothing `type` does not; the two things it stood in for are facets now (kettlebell/mace/jump_rope → `equipment`, martial_arts/athletic → `tradition`). `category` stays because push (26), pull (22) and legs (29) are a movement pattern and no facet holds one — §10.1 says a legacy field keeps its name until its replacement can carry the information, which is a different thing from keeping it because it is old — `scripts/facet-tags.mjs`, `docs/TAXONOMY.md` §10.2.
- 2026-08-29 — the library's equipment dropdown was offering prose as options: the free-text `equipment` field holds 263 enum-ish values and 71 sentences written for a reader, and the screen listed anything under 24 characters. Now the `equipment` facet, ids with labels; the prose stays in `fields.tool` where it is instruction rather than a filter — `src/app/ui/viewLibrary.js`.

- 2026-08-29 — **the 28 Aug "body-work cards carry no `muscles`" entry, closed — and it had a cost that was visible before anyone measured it.** Kevin: "I looked earlier for a calf release and found none." There are four. `bw-calf` is a release card named **"Calves"** with no anatomy field at all, so the library's muscle filter could not return it. The fold-in could not fix it either — a translation needs something to translate. `anatomy-tags.json` tags 49 such items directly and records 15 as `noTarget` with a reason; coverage 289 → 340 of 378, and the self-tests got their wires (Knee to wall now says it measures `ankle-dorsiflexion` and `soleus`). The library filter reads the graph now rather than the free-text field, and the graph ships with the catalogue — `src/content/vocab/anatomy-tags.json`, `src/app/ui/viewLibrary.js`, `docs/TAXONOMY.md` §3.3.
- 2026-08-29 — a content gap that is now a number instead of a feeling: filtering on `hamstrings` returns 22 items and **one** release, against four for calves. Kevin's report is that those two are the tightest things he has left — `src/content/library.json`.
- 2026-08-29 — **`levels` was doing the work of four different things.** From one dictation about Legs Up the Wall: a rung (prop under the heel) belongs in `levels`; a *variation* that changes the effect (one leg down turns `calm` into `lengthen`) needs its own item, or the ledger records a stretch as downregulation; a *prerequisite* ("release the psoas first or the down leg torques the hip") is a fourth item relation alongside pair/swap/stack; and an *addition* carries its own epistemic status. Modelled as `variationOf` and `before`, both checked by the build — `docs/TAXONOMY.md` §6.7, `src/content/authored/variations.json`.
- 2026-08-29 — **the suite caught the author's name reaching a stranger.** The first draft of the two variation cards attributed them in `sourceNote` by name; `namesTheAuthor` in `tests/screens.test.js` failed the build, as PLAN §1 requires. Rewritten to name what the item is based on and never who based it. The guard was written 28 Aug for the shipped day content and it held for content authored a day later — `src/content/authored/variations.json`.

- 2026-08-29 — **a prerequisite with no condition is a caution that presumes one.** `before: ["bw-hip"]` on the one-leg-down wall stretch told every reader to release their psoas first. Kevin: "the psoas release is only a prerequisite if your psoas is tight, and if you are dropping one leg. If both are up the psoas isn't a factor." The second clause was already handled — `before` sits on the variation, not the parent — but the first was a modelling error, and it is the same shape as commit `043f266`, the five cards that told the reader which conditions they have. Cost is identical: friction handed to people it is not for, who skip the movement rather than the step. `before` is `{ item, when? }` now, and `build-catalog` names every unconditional one — `src/content/authored/variations.json`, `scripts/build-catalog.mjs`, `docs/TAXONOMY.md` §6.7.
- 2026-08-29 — the condition wants a test and there isn't one: the catalogue has no hip-flexor-length test (`test-hipflex` measures hip flexion range, a different question), so the `when` names the observation instead — the lower back lifting off the floor. The `test` field on a prerequisite exists and is empty, which is the honest state rather than wiring it to a test that measures something else. A Thomas-test-style item is a real content gap — `src/content/library.json`, `docs/TAXONOMY.md` §5.

- 2026-08-29 — **`new Set()` on a string filed 334 items under their own letters.** The equipment derivation seeded from `out.equipment` directly; for the 334 items whose `equipment` is prose rather than an enum value, that iterated characters, so `bw-ham` shipped with `equipment: [" ","F","a","b","e","f","i","l","m","o","r"]` and the library's dropdown offered single letters. Shipped in the tagging commit and found by reading a card, not by a test. A string is iterable and that is exactly why the check has to be explicit — prose now goes to `fields.tool` where it is instruction rather than a filter, and 70 "values" became 13 — `scripts/facet-tags.mjs`, `tests/catalog.test.js`.
- 2026-08-29 — the hamstring gap is closed: four releases against one, matching the calf. The useful one is `release-hamstring-nerve-or-muscle` — a hamstring stretched daily for years that has never changed may be sciatic tension rather than a short muscle, which the chin-tuck test tells apart, and for which more stretching is the wrong answer. It gates the other releases as a conditional prerequisite rather than sitting beside them — `src/content/authored/release-hamstring.json`.

- 2026-08-29 — **the first self-test that can route.** `test-hipflexor-length` carries `outcomes`: four readings, each with what it looks like, what it means, the anatomy it implicates and the item to do about it — psoas/iliacus, rectus femoris, or TFL, three muscles wearing one complaint and released in three different places. It was written because a prerequisite needed it (the one-leg-down wall stretch's `test` field was specified and empty). `build-catalog` refuses an outcome pointing at a node or item that does not exist, and prints the ratio: **1 of 14 self-tests can route** — `src/content/authored/self-tests.json`, `docs/TAXONOMY.md` §5.1.
- 2026-08-29 — **the fourteenth self-test ships with the thirteen others' defect, and this one makes the cost legible.** `tracking: 'check'` on a test whose result is a four-way reading per side (GAPS §3, D36). The `outcomes` carry the meaning; nothing carries the answer, so a person can read what their result means and cannot record which result they had. A measurement tracking type is the next thing this needs, and it is now blocking something concrete rather than being a line in a register — `src/content/authored/self-tests.json`, `docs/GAPS.md` §3.

- 2026-08-29 — **D36 closed: all fourteen self-tests record a real reading, on a re-test interval.** `tracking: 'measure'`, a `measure` spec (number / scale / choice), and `log[itemId].readings.{left|right|both}` beside the checks. Per side, because a left ankle and a right ankle are two measurements rather than one averaged; zero is a reading and a cleared one is absent (ruling A); a choice keeps the outcome's words at the time (D20). Closes `GAPS.md` §3's "no measurement recorded, no 2–4-week cadence, no delta" — the first two of the three — `src/app/trackerOps.js`, `src/app/ui/viewToday.js`, `docs/TAXONOMY.md` §5.2.
- 2026-08-29 — **every unit and every re-test interval in the catalogue was already written down, in a field nothing could read.** `notice` said "Recorded in cm." and `why` said "Re-test: Every 2 weeks. Higher is better." on all thirteen imported tests. The specs are parsed from those two sentences rather than authored, and a measurement whose card says neither stops the build — a guessed unit is indistinguishable from a chosen one (canon 3.7). The sentences are then stripped from `why`, which was never a why — `scripts/measure-specs.mjs`.
- 2026-08-29 — still open after this: the **delta**. `GAPS.md` §3 names three things D36 needs — a recorded measurement, a cadence, and a sparkline showing the change. Two are built; nothing yet shows a person their own trend, which is the reason to take a reading twice — `docs/GAPS.md` §3.

- 2026-08-29 — **D36 fully closed: the delta ships.** `src/lib/readings.js` — series, summary and sparkline as pure functions. Three refusals to overstate are pinned by tests: one reading says "nothing to compare it to yet" rather than drawing a flat line; direction comes from the item's own `better` and an unlabelled change is reported without a verdict; the dates are carried, so three readings across six months cannot read as steady progress. The picture is plotted by position rather than by date and is `aria-hidden`, because everything it shows is in the sentence beside it — spacing a fortnight and six months identically along an axis is a lie a picture can tell that a sentence cannot. `GAPS.md` §3's three-part D36 row is now complete — `src/lib/readings.js`, `src/app/ui/viewToday.js`, `docs/TAXONOMY.md` §5.3.

- 2026-08-29 — **the referral map ships (D37): 6 sites, 20 candidates, answering the search box.** Edges between anatomy nodes, never properties of an item — "elbow pain often comes from the shoulder" is not a fact about any exercise. The build refuses five things, each a way the map could go quietly useless: a source pointing at a node that does not exist, a candidate routing to an item that is not there, a candidate with no tell, a candidate with no graded evidence, and **a site with no red flags** — a person arriving at a symptom list is arriving with a symptom — `src/content/referral.json`, `scripts/build-catalog.mjs`, `docs/TAXONOMY.md` §4.1.
- 2026-08-29 — grading per edge rather than per file is what makes the map honest: 11 of 20 candidates are `established` and 9 `exploratory`, sitting side by side. At the outer elbow the forearm extensors say they are the commonest source, the thoracic-outlet pattern says "worth a look, not a conclusion", and the postural-chain candidate says outright that it is "plausible, cheap to try, weak to claim". A file-level disclaimer would have flattened all three into one hedge and taught nothing — `src/content/referral.json`.

- 2026-08-29 — **faceted browse built; the library screen lost two mechanisms and gained one.** It had a chip row for `kind` plus a muscle dropdown plus an equipment dropdown — three parallel filters, and three only because the facets did not exist yet. Now five slices with one on show at a time, composing rather than replacing each other, with every chosen value visible as a removable pill. Chip counts are computed against the other filters AND the search text, so a chip reads "4 releases for your calf" rather than "81 releases" and can never promise more than the query has left — pinned by a test — `src/app/ui/viewLibrary.js`, `docs/TAXONOMY.md` §8.1.
- 2026-08-29 — a test-writing trap worth recording: `textContent` concatenates without separators, so a chip ending in "9" followed by a result count of "84 of 383" matched `/(\d+) of \d+/` as **984**. The assertion failed for the right reason and the wrong one at once. Counts are read from a `.result-count` element now rather than from the page's text — `tests/screens.test.js`.

- 2026-08-29 — **`category` retired; the seven questions it answered all have fields now.** `pattern` (11 values, closed) was the last thing it held alone. Both `kind` and `category` are read from at build time and dropped from the artifact — the sources keep them, so the derivations go on working. No shipped item carries either, and a test pins it — `scripts/facet-tags.mjs`, `docs/TAXONOMY.md` §10.3.
- 2026-08-29 — **a substring rule got 10 of 148 patterns wrong, and reading them was the only way to know.** `Hop` matched the middle of "Woodchop"; `Press` made a leg press a pushing pattern; `Cross` turned the skipping drill "Criss-Cross" into a punch; `Curl` made the Nordic hamstring curl a pull; Pallof Press came out as push when resisting rotation is the whole exercise. The name is the definitive signal for this facet and that is exactly what made the near-misses invisible. All ten are marked `corrected` in the file with the call written down — `src/content/vocab/pattern-tags.json`.
- 2026-08-29 — **the browse was local and the referral map only answered symptom searches, so the two never met.** Kevin: "the person looking to stretch or release something in their leg may not realise there is something in their hip/glute/back that is pulling on it." Browsing a region now surfaces what refers into it from outside — sources outside the region only, since the local ones are already in the results. Neither system changed; they share the anatomy vocabulary, which is the whole argument for one join key — `src/app/ui/viewLibrary.js`, `docs/TAXONOMY.md` §10.3.

- 2026-08-29 — **the `effect` vocabulary settled, and the tagging answered the question rather than a preference.** Nine values, no merges and no cuts: `mobilise` vs `lengthen` was decided by the mobility shelf splitting 23 held positions from 11 driven through range; `condition` survives because `full body` was four items with no other tag anywhere; `activate` has **zero** overlap with `load` across 16 items, so it is not light loading. One near-miss recorded rather than built: Dead Hang reads as `load` where it is really decompression, and one item does not earn a value (§3's guard) — `src/content/vocab/facets.json`, `docs/TAXONOMY.md` §2.3.1.
- 2026-08-29 — **what needed deciding was not the list but `counts`.** Measured by the kind of node each effect points at: release/lengthen/load/activate/mobilise/condition 0% systemic, control 38%, calm 57%, circulate 73%. Six count per anatomy node, three do not. This reconciles FRAMEWORK's four ledger roles (released, strengthened, stretched, balanced) with the measured nine — the content demanded two more of that kind and three that are not per-muscle at all. `condition` is systemic despite measuring 0%, because a rope session tags the calves and counting that as coverage would let the pairing law believe a debt was paid that was not: what an effect touches and what it covers are different questions — `docs/TAXONOMY.md` §2.3.2.
- 2026-08-29 — found while measuring: the shipped `catalog.anatomy` was slimmed to id/name/parents, so nothing consuming the catalogue could tell a system node from a muscle — which is precisely what `counts` needs. `kind` now ships with it — `scripts/build-catalog.mjs`.

## 3 · Expectation → outcome ledger (canon 4.1)

Write the expectation line **before** a build or judgment session's work; append the outcome after. **Forward-only from Aug 18** — no backfilled expectations, because a backfilled expectation is fake pre-registration.

Format: `date — expected: … → actual: …`

- 2026-08-20 — expected: framework review closes most open design items, surfaces ≥2 unlogged roadmap conflicts, produces market-validated feature shortlist → actual: 4 collisions surfaced and reconciliation agreed; ~20 rulings logged across two rounds incl. PEM baseline-relative model (pattern-not-symptom-list, immediate≠delayed), vocabulary-as-open-census with introduced-on semantics, training-log progress foundations, focus-list data layer, streak shape (self-set floors, free rest, retroactive pause, computed-view architecture); 1 design line still open (pacing profile × too-hard arithmetic). *[Pasted 2026-08-21 from the Aug-20 rulings file; the expectation half was captured same-session Aug 20 — whether it was stated before the session's work began is unverified from the record, annotated here per this section's forward-only rule rather than presented as clean pre-registration.]*
- 2026-08-21 — expected (Claude's proposal at R1 session open, before any ruling; not separately ratified by Kevin): four collisions named/settled; all 8 disposition rows ruled; composer-unification call made explicitly; read-first chain ratified; roadmap v1.7 drafted same-session; PLAN.md diff + GAPS.md escape to CC homework → actual: R1 closed same-session, but not along the packet's path — formal collision-naming dropped as overcomplication after Kevin's pushback (triage: 6 rows derived from existing rulings, 2 genuinely open); one composer adopted (D16 amended); rating + limiting-factor model ruled (D42), which dissolved the pacing-arithmetic open item rather than answering it — a design gain the expectation didn't predict; D22 extended to training logs by default-adoption; v1.7 + FRAMEWORK v3 + this file drafted same-session; PLAN.md diff + GAPS.md to CC as expected; two unpredicted catches (OD-1 already ruled; D5/not-in-v1 seam) via the mining the packet had deferred.

- 2026-08-28 — expected: S0b is mostly mechanical. The freeze should be a no-op on content (the generator was already verified to reproduce `library.json` byte-identically, hash `53c4e786b5aa`), so the real work is the authored path: a merge step that fails loudly on duplicate ids and parse errors, `build-library.mjs` out of the deploy chain, and `vestibular.json` as the format's first test. I expect the PT draft to surface three to five field mismatches, most of the "ships but renders nowhere" kind rather than the "silently dropped" kind — `tier`, `role` and `evidence` have no render path, and `fields.carefulAudience` sits inside a block the validator filters to five known keys, so that one should be a genuine silent drop. The two validator additions I expect to be small in `protocolFile.js` and larger than they look in `viewLibrary.js`, because "use the chosen level" needs a chooser that does not exist yet. Suite 135 → I expect roughly 145–150. Known risk: `Project_Gotchas_v1.md` is not in the repo, so step 1.3 has no target and I will have to create it or stop and ask. → actual: done, and the shape of the estimate held better than its numbers. The freeze was the no-op predicted — `imported-legacy.json` is the generator's output at hash `53c4e786b5aa`, frozen from `master@4f631ae`, and `build-catalog.mjs` now merges it with `src/content/authored/*.json` into the catalogue the browser reads; both fail-loud paths (duplicate id, unparseable file) halt naming the file, and both are tested rather than demonstrated. Suite 135 → 148, at the bottom of the predicted range because the merge logic needed extracting into importable functions before its failure paths could be tested at all — which the expectation did not anticipate and which was the right call anyway. The PT draft surfaced four field findings, one more than the low end: `tier`/`role`/`evidence` ship inert as predicted, `fields.carefulAudience` is the genuine silent drop as predicted, and the unpredicted one is that a catalogue item's own `target` is ignored by the add flow, so the PT's 30-second drills would have landed in a day saying 45. `viewLibrary` was indeed the larger half: the chooser did not exist and now does, verified in the running app rather than only in jsdom — choosing rung 2 stores `activeLevel: 2` and the day gets rung 2's instructions. `Project_Gotchas_v1.md` did not exist and was written rather than located, on the `GAPS.md` precedent; flagged rather than treated as routine. One thing deliberately not done: `npm run library` still writes to `library.json` and will silently clobber every authored item, because retargeting it was not on the task list — Gotchas §4, Kevin's call.

## 4 · Runway check — monthly (canon 5.7)

One question, on a schedule, not when it forces itself: *is this work still net-positive against capacity and the simpler alternative?* Cadence: monthly [FEASIBILITY: interval underived; adjust freely]. First check due ~2026-09-18.

Format: `date — still net-positive? — one line why`

*(first entry due mid-September)*
