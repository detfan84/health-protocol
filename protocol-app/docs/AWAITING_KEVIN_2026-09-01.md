# What's waiting on Kevin — 1 Sep 2026

*Compiled by a three-way sweep over the repo, both git branches, and the session
transcripts. Rulings are cited by what they say, not by number. Each item names
where it's recorded. The Connection map leads because it was asked about
directly — and because the answer is not what the docs imply.*

---

## 1 · The Connection map: there is nothing here to review

The map you're supposed to review is **not in this repository and never was**.
All 100 commits on both branches were searched: no Connection map, no Map Edit
Log, no Recognition Quiz Roadmap was ever committed. The committed docs cite
all three as authoritative, but the documents themselves live wherever the
Aug-18 quiz-design session happened — most likely the Windows machine
(`C:\Users\kevin\Health App\`, the path the handoff records) or a claude.ai
Project. Same class of missing: FRAMEWORK_v3's marked-up original, roadmap
v1.8, the personal protocol doc.

**What the map is:** the symptom→muscle→module chain document the Recognition
Quiz's question tree walks. Ten sections, §0–9. You reviewed §0–2 in August and
signed them off; §3–9, the pass grading which claims carry which confidence,
and the module re-staging are the outstanding work. The quiz — the fourth
output of the assessment, module toggles — is built the day this lands, and not
before.

**What leaks through the citations** (so you know which document you're looking
for): its Edit Log rules PHQ-8 into the quiz (§A.1, Aug 18) · adds a
breath/BOLT sub-module (D4) · adds a floor-level contrast-therapy module, bowls
plus shower finish (D5) · and settles survey/export scope (E1–E3: feedback
mailto in, in-app survey layer out of v1, one generic anonymous answer-set
export shared by quiz and doctor summary).

**The decision, then, is not §3–9 yet — it's the document itself:**

- **(a) Locate it.** If it's on the Windows machine or in a claude.ai Project,
  commit it here and the §3–9 review becomes real work I can stage for you —
  section by section, drafted audit, you strike.
- **(b) Reconstruct it.** If it can't be found, I draft a reconstruction from
  everything the committed docs quote of it, plus the referral chains already
  shipping in the app (20 chains, 11 established), and it becomes a new v1.0
  you mark up. Slower and lossier than finding the original.

The repo has precedent: GAPS.md was cited in three places, turned out never to
have existed, and your ruling was locate-or-strike — it got written. The
difference here is the map is your review target, so the original matters more.

**One seam you can rule on today regardless** (flagged "[your] line at next
touch," and this is a touch): the Edit Log's contrast-therapy module (D5)
versus FRAMEWORK's "cold exposure not in v1." Never reconciled. The recorded
recommendation: park the contrast module to v1.1 — the later document governs
and its evidence labels are thin. Strike the parking to ship it instead.

---

## 2 · Markup passes — reading, striking, done

Things drafted from your rulings that become yours when you've crossed out
what's wrong:

1. **The Learn draft** (`LEARN_DRAFT_v0.md`) — revised today around the core
   principle you just gave. §0 is the fair-warning on-ramp; you also pick its
   shape: onboarding screen, or dismissible card at the first release item.
2. **FRAMEWORK v3** — everything in it was ruled in August; the gaps register
   calls the markup "largely ceremonial." Reading it through closes Phase 0's
   last formal gate.
3. **METHODOLOGY** — asks you two direct questions: is "notice is the
   instrument" a real principle or a card field promoted too far; and the
   missing mechanics of breath-as-a-stretching-force (where it applies, how
   many breaths, what tells you it's opening).
4. **TAXONOMY** — the shape is settled and built against; the vocabularies
   inside (nine effect values, demands, patterns) await your read.
5. **The schema store list** for the day-engine extension — derived from your
   rulings, flagged strike/adjust, and it must be reviewed *before* the next
   migration rung ships, because released rungs are never edited. (Findings
   store already shipped with your line; this is the rest.)
6. **Four anatomy rows** flagged for a human eye — the unqualified "tibialis"
   string, "outer thigh," and two more like them.

## 3 · Content decisions — nobody else can say what these hold

7. **The day-arc presets** — your own ask (29 Aug): recommended days at ~7, 20,
   40, 70 minutes. What's blocking: *what's in them.* The estimates layer (1
   Sep) can now propose contents-to-budget as a starting draft — say the word
   and I'll deal four candidate presets for you to strike.
8. **The faith layer** — yes/no on the note-first opening as specced, and how
   many reflections it ships with.
9. **The achievements catalog** — mechanism ruled; the milestone list was
   parked for its own session with you.

## 4 · The one only your hands can do

10. **Use the app for real, on your phone.** Phase 1's done-when is your daily
    protocol running entirely on the new build with nothing lost in the round
    trip — and the loop counter still reads zero. Every open design question
    gets easier to judge after a few real days. It's live, and Android runs it
    in an ordinary tab.

## Ratified today, now on record

- **Template slate waits for a fuller library** — you agreed with your own
  August ruling ("it's encouraging when I agree with myself").
- **The core principle** is stored verbatim in
  `VOICE_Core_Principle_2026-09-01.md`, including the injury boundary (the
  ACL point) as the teaching's other half.
